# Billing service port
PORT=4010

# Optional comma-separated CORS allowlist for browser calls
# CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# Shared secret expected in x-service-token or Authorization: Bearer <token>
BILLING_SERVICE_TOKEN=change-me

# Optional explicit backend path used for shared modules
# BILLING_BACKEND_PATH=../backend
